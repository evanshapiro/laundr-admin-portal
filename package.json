{
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "dependencies": {
    "mobx": "^6.0.1",
    "mobx-react": "^7.0.0"
  },
  "scripts": {
    "postinstall": "npm install --prefix ./client"
    "start": "npm start --prefix ./client"
  }
}
