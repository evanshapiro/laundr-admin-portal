{
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "dependencies": {
    "mobx": "^6.0.1",
    "mobx-react": "^7.0.0"
  },
  "scripts": {
    "start": "npm start --prefix client"
  }
}
